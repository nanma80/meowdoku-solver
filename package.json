{
  "name": "meowdoku-solver",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/server.mjs",
    "test": "node --test tests/*.test.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "playwright": "1.62.1",
    "pngjs": "7.0.0",
    "prettier": "3.6.2"
  }
}
